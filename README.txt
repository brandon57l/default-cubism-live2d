# Default Cubism Live2D

This project is a simple implementation of Live2D.

## Installation

Make sure you have **Node.js** installed, then run the following command to install a simple HTTP server:

```sh
npm install -g http-server
```

## Running the Application

```sh
http-server
```
